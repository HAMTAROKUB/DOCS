{
  "buildCommand": "cd webadmin-DOCS && npm install && npm run build",
  "outputDirectory": "webadmin-DOCS/dist",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
